<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <PackageId>ChatApp.Protocol.Tcp.Json</PackageId>
    <AssemblyName>ChatApp.Protocol.Tcp.Json</AssemblyName>
    <RootNamespace>ChatApp.Shared.Protocol.Tcp.Json</RootNamespace>
    <Title>ChatApp TCP protocol JSON metadata</Title>
    <PackageTags>ChatApp;contracts;TCP;protocol;JSON</PackageTags>
    <Description>BCL-only System.Text.Json metadata and policy for ChatApp TCP contracts.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ChatApp.Protocol.Tcp\ChatApp.Protocol.Tcp.csproj" />
  </ItemGroup>
</Project>
